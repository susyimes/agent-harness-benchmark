# VISION

## 一句话

`agent-harness-benchmark` 测的不是模型单次回答能力，而是 **模型 + harness + 工具 + 执行环境** 在真实工程任务里的系统级效果。

我们关心的问题不是：

> 这个模型会不会写一段代码？

而是：

> 一个 agent 系统能不能读懂现有工程、定位问题、调用工具、运行测试、修复回归，并稳定交付一个可审查的结果？

## 为什么需要这个项目

大多数传统 benchmark 更擅长测静态能力：选择题、数学题、单文件代码题、一次性问答。

但 agent framework 的核心价值不在一次回答里，而在完整执行链路里：

- 能否理解已有仓库，而不是只生成孤立代码
- 能否主动运行测试，而不是只假设代码正确
- 能否根据失败信息迭代，而不是一次失败就结束
- 能否补回归测试，而不是只把当前公开测试做绿
- 能否控制修改范围，而不是用大重构掩盖问题
- 能否记录成本、耗时、工具调用与失败循环，而不是只报告最终分数

因此，我们需要一套更贴近真实工程工作的 benchmark。它应该小到本地可跑，硬到能拉开差距，清晰到别人能复现。

## 核心理念

### 1. 评测对象是系统，不只是模型

同一个模型，裸调用和经过 harness 编排后的表现可能完全不同。

本项目鼓励做对照实验：

- 裸模型：单轮回答、无工具、无调试循环
- harness agent：多轮执行、工具调用、测试反馈、反思修复、交付总结

真正要衡量的是 harness 是否把模型变成了更可靠的工程执行系统。

### 2. 真实工程闭环比孤立题目更有价值

一个好的 agent benchmark 应该包含：

- 已有代码结构
- 预埋缺陷
- 公开测试
- 隐藏用例
- 可运行命令
- 可比较评分
- 可审查提交

我们不追求题目花哨，而追求任务像真实工作：读代码、改代码、跑测试、修 bug、补测试、写总结。

### 3. 过程质量也是结果的一部分

只看最终是否通过测试是不够的。

一个 agent 可能通过删除测试、硬编码输出、扩大修改范围来“得分”。因此本项目同时记录：

- 是否先复现问题
- 是否有无效循环
- 是否补了回归测试
- 是否引入新风险
- 是否留下清晰交付说明
- 是否控制了 token、耗时和工具调用成本

这让评测更接近真实团队对 agent 的要求。

### 4. 标准框架兼容，实战任务优先

项目采用双轨思路：

- 标准 harness 框架测试法：兼容 Inspect AI、lm-evaluation-harness、OpenAI Evals 等生态，方便复现和横向比较
- 实战测试法：维护多语言真实工程任务，重点测试 agent 的工程闭环能力

标准框架提供可信度，实战任务提供区分度。

### 5. 小而硬，本地可跑，可持续扩展

每个任务都应尽量满足：

- 零外部服务
- 少量或零数据准备
- 本地一条命令可验证
- 难度中高
- 有公开与隐藏考点
- 能稳定复现初始失败和最终结果

我们宁可先把少数高质量任务做扎实，也不堆很多无法复现的题目。

## 项目当前形态

当前仓库维护两类实战任务：

- Runner 家族：Python、Node、Go、Kotlin
  - 测 DAG、pipeline、retry、timeout、cancel、report、并发稳定性
- Log / Query 家族：Java、Swift
  - 测 parser、search、filter、stats、redaction、CLI/API 一致性

短期优先把 Python 和 Node 做成第一批稳定可跑的基准题，再扩展到其他语言。

## 适合贡献什么

你可以从以下方向参与：

- 完善某个语言的 `repo-template`
- 固定初始失败测试数和公开验证命令
- 设计隐藏测试，但不暴露给参测 agent
- 增强 benchmark 数据规模和性能场景
- 补充评分脚本和结果记录模板
- 接入 Inspect AI 等标准评测框架
- 编写 bare-model vs harness-agent 的对照流程
- 改进文档，让新使用者更容易跑起来
- 提交一次真实 agent framework 的运行记录

每个贡献都应该尽量落到一个可复现产物上：一个任务、一个测试、一个 scorer、一个 runner、一个报告模板，或一次完整评测记录。

## 非目标

本项目暂时不追求：

- 做通用模型排行榜
- 复刻所有已有 benchmark
- 只测算法题或选择题
- 依赖复杂云环境
- 用不可复现的人工主观评价当核心指标

我们更关心 agent 在真实工程工作里的可靠性、成本和交付质量。

## 给贡献者的一句话

如果你相信未来的 AI 编程能力不只来自更强模型，也来自更好的 harness、工具编排、环境反馈和评测协议，那么这个项目就是为你准备的。

我们要一起做的不是又一个分数榜，而是一套能诚实回答这个问题的工程基准：

> 这个 agent 系统，真的更会干活了吗？
